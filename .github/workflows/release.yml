name: Build TigerBeetle for Debian

on:
  workflow_dispatch:
    inputs:
      tigerbeetle_version:
        description: The version of TigerBeetle to build.
        type: string
        required: true
      tigerbeetle_release_suffix:
        description: The upstream release suffix appended to the asset URL (e.g. '1' gives download path '0.17.7-1'). Leave empty for releases without a suffix.
        type: string
        required: false
        default: '1'
      build_version:
        description: The build version.
        type: string
        required: true
      architecture:
        description: Architecture to build (amd64, arm64, or 'all' for all architectures)
        type: choice
        default: 'all'
        options:
          - 'all'
          - 'amd64'
          - 'arm64'

permissions:
  contents: write

jobs:
  build-tigerbeetle:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout tigerbeetle-debian
        uses: actions/checkout@v4

      - name: Build TigerBeetle
        run: ./build.sh ${{ inputs.tigerbeetle_version }} ${{ inputs.build_version }} ${{ inputs.architecture }} ${{ inputs.tigerbeetle_release_suffix }}

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifact
          path: tigerbeetle_*.deb

  build-src:
    name: Build Source Packages
    runs-on: ubuntu-latest
    steps:
      - name: Checkout tigerbeetle-debian
        uses: actions/checkout@v4

      - name: Install dpkg-dev
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends dpkg-dev devscripts wget ca-certificates

      - name: Build source packages
        run: ./build_src.sh ${{ inputs.tigerbeetle_version }} ${{ inputs.build_version }}

      - name: Upload Source Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: source-artifact
          path: |
            tigerbeetle_*.dsc
            tigerbeetle_*.orig.tar.gz
            tigerbeetle_*.debian.tar.xz

  release-tigerbeetle:
    name: (Draft-)Release TigerBeetle for Debian
    if: github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: [build-tigerbeetle, build-src]
    steps:
      - name: Download All Artifacts
        uses: actions/download-artifact@v4
        with:
          name: artifact

      - name: Download Source Artifacts
        uses: actions/download-artifact@v4
        with:
          name: source-artifact

      - name: Publish Release Draft
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          files: |
            tigerbeetle_*.deb
            tigerbeetle_*.dsc
            tigerbeetle_*.orig.tar.gz
            tigerbeetle_*.debian.tar.xz
          name: ${{ inputs.tigerbeetle_version }}+${{ inputs.build_version }}
          tag_name: ${{ env.RELEASE_TAG }}
          fail_on_unmatched_files: true
